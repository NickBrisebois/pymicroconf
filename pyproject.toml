[project]
name = "pymicroconf"
version = "0.1.0"
description = "A Python library for managing configuration files using TOML and environment variables"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
